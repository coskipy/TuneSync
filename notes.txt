To sync playlist using spotdl:
    1. If sync folder does not exist: 
        spotdl sync 'url' --save-file [folder path]/SyncData.spotdl --output [folder path]
    2. To sync existing folder
        spotdl sync SyncData.spotdl